---
// This page will redirect to the appropriate language version
// Based on browser language detection or fallback to English
---

<script>
  // Client-side language detection and redirect
  function detectLanguageAndRedirect() {
    // Get browser language
    const browserLang = navigator.language || navigator.languages?.[0] || 'en';
    
    // Map browser languages to supported languages
    const supportedLangs = ['en', 'zh'];
    let targetLang = 'en'; // default
    
    // Check if browser language matches supported languages
    if (browserLang.startsWith('zh')) {
      targetLang = 'zh';
    } else if (browserLang.startsWith('en')) {
      targetLang = 'en';
    }
    
    // Redirect to the appropriate language version
    window.location.href = `/${targetLang}/`;
  }
  
  // Run detection on page load
  detectLanguageAndRedirect();
</script>

<noscript>
  <!-- Fallback for users with JavaScript disabled -->
  <meta http-equiv="refresh" content="0; url=/en/">
</noscript>

<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>C++ Learning Hub</title>
  <meta name="description" content="Master C++ programming from basics to advanced competitive programming">
</head>
<body>
  <div style="text-align: center; padding: 2rem; font-family: system-ui, sans-serif;">
    <h1>🚀 C++ Learning Hub</h1>
    <p>Redirecting to your preferred language...</p>
    <p>
      <a href="/en/">English</a> | 
      <a href="/zh/">中文</a>
    </p>
  </div>
</body>
</html>
